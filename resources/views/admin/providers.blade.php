@extends('layouts.app')
@section('content')
<div class="top"><div><div class="eyebrow">Operations / Connectivity</div><div class="h1">SMPP Providers</div><div class="sub">Configure provider credentials, buy rates and billing mode.</div></div><a class="button" href="{{ route('dashboard') }}">← Dashboard</a></div>
@if(session('success'))<div class="notice">{{ session('success') }}</div>@endif @if($errors->any())<div class="error">{{ $errors->first() }}</div>@endif
<section class="card"><h2>Add provider</h2><form method="POST" action="{{ route('admin.providers.store') }}">@csrf<div class="formgrid"><label>Name<input name="name" placeholder="Carrier A" required></label><label>Host<input name="host" placeholder="smpp.provider.com" required></label><label>Port<input type="number" name="port" value="2775" required></label><label>System ID<input name="username" required></label><label>Password<input type="password" name="password" required></label><label>Buy rate<input type="number" step="0.000001" name="buy_rate" value="0" required></label><label>Country<input name="country"></label><label>Priority<input type="number" name="priority" value="100" required></label><label>Billing mode<select name="billing_mode"><option>SUBMISSION</option><option>DLR</option></select></label></div><button class="button" type="submit">Save provider</button></form></section>
<section class="card" style="margin-top:15px"><h2>Provider list</h2><div class="tablewrap"><table><thead><tr><th>Name</th><th>Host</th><th>Port</th><th>System ID</th><th>Status</th><th>Buy rate</th><th>Billing</th><th>Priority</th></tr></thead><tbody>@forelse($providers as $provider)<tr><td>{{ $provider->name }}</td><td>{{ $provider->host }}</td><td>{{ $provider->port }}</td><td>{{ $provider->username }}</td><td><span class="tag">{{ $provider->status }}</span></td><td>{{ number_format($provider->buy_rate, 6) }}</td><td><span class="tag">{{ $provider->billing_mode }}</span></td><td>{{ $provider->priority }}</td></tr>@empty<tr><td colspan="8">No providers yet.</td></tr>@endforelse</tbody></table></div>{{ $providers->links() }}</section>
@endsection
