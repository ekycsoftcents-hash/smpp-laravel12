@extends('layouts.app')
@section('content')
<div class="top"><div><div class="eyebrow">Operations / Routing</div><div class="h1">Routing Engine</div><div class="sub">Create prefix, country and priority routing rules.</div></div><a class="button" href="{{ route('dashboard') }}">← Dashboard</a></div>
@if(session('success'))<div class="notice">{{ session('success') }}</div>@endif @if($errors->any())<div class="error">{{ $errors->first() }}</div>@endif
<section class="card"><h2>Add routing rule</h2><form method="POST" action="{{ route('admin.routing.store') }}">@csrf<div class="formgrid"><label>Provider<select name="provider_id" required>@foreach($providers as $p)<option value="{{ $p->id }}">{{ $p->name }}</option>@endforeach</select></label><label>Customer<select name="customer_id"><option value="">All customers</option>@foreach($customers as $c)<option value="{{ $c->id }}">{{ $c->name }}</option>@endforeach</select></label><label>Type<input name="type" value="SMS" required></label><label>Country<input name="country"></label><label>Prefix<input name="prefix"></label><label>Strategy<select name="strategy"><option>priority</option><option>lcr</option><option>round_robin</option></select></label><label>Priority<input type="number" name="priority" value="100" required></label></div><button class="button" type="submit">Save rule</button></form></section>
<section class="card" style="margin-top:15px"><h2>Routing rules</h2><div class="tablewrap"><table><thead><tr><th>Provider</th><th>Customer</th><th>Type</th><th>Country</th><th>Prefix</th><th>Strategy</th><th>Priority</th><th>Enabled</th><th>Actions</th></tr></thead><tbody>@forelse($rules as $rule)<tr><td>{{ $rule->provider_name }}</td><td>{{ $rule->customer_id ?? 'All' }}</td><td>{{ $rule->type }}</td><td>{{ $rule->country ?? '*' }}</td><td>{{ $rule->prefix ?? '*' }}</td><td>{{ $rule->strategy }}</td><td>{{ $rule->priority }}</td><td><span class="tag">{{ $rule->enabled ? 'YES' : 'NO' }}</span></td><td><details><summary class="button">Edit</summary><form method="POST" action="{{ route('admin.routing.update', $rule->id) }}" style="min-width:520px;padding:12px">@csrf @method('PUT')<div class="formgrid"><label>Provider<select name="provider_id" required>@foreach($providers as $p)<option value="{{ $p->id }}" @selected((int)$rule->provider_id === $p->id)>{{ $p->name }}</option>@endforeach</select></label><label>Customer<select name="customer_id"><option value="">All customers</option>@foreach($customers as $c)<option value="{{ $c->id }}" @selected((int)$rule->customer_id === $c->id)>{{ $c->name }}</option>@endforeach</select></label><label>Type<input name="type" value="{{ $rule->type }}" required></label><label>Country<input name="country" value="{{ $rule->country }}"></label><label>Prefix<input name="prefix" value="{{ $rule->prefix }}"></label><label>Strategy<input name="strategy" value="{{ $rule->strategy }}" required></label><label>Priority<input type="number" name="priority" value="{{ $rule->priority }}" min="1" required></label><label>Enabled<select name="enabled"><option value="1" @selected($rule->enabled)>Yes</option><option value="0" @selected(!$rule->enabled)>No</option></select></label></div><button class="button" type="submit">Save changes</button></form></details><form method="POST" action="{{ route('admin.routing.destroy', $rule->id) }}" style="display:inline" onsubmit="return confirm('Delete this rule?')">@csrf @method('DELETE')<button class="button" style="background:var(--danger)" type="submit">Delete</button></form></td></tr>@empty<tr><td colspan="9">No routing rules yet.</td></tr>@endforelse</tbody></table></div>{{ $rules->links() }}</section>
@endsection
